import React, { PropTypes } from 'react';

const <%= name %> = (props) => {
  return (
    <div>
      ---AUTOGENERATED <%= name %>---
    </div>
  );
};

<%= name %>.propTypes = {
};

<%= name %>.defaultProps = {
};

export default <%= name %>;
